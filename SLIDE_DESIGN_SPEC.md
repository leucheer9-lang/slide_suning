# GEO 2026 幻灯片设计与排版规范 (Slide Design Specifications)

本文件定义了当前幻灯片项目核心设计与排版规范。在调整或开发幻灯片页面时，必须严格遵守以下规范。

## 1. 尺寸比例与缩放关系
* **设计稿分辨率 (Figma)**: `3840px × 2160px` (4K 比例)
* **前端代码分辨率 (React)**: `1920px × 1080px` (1080p 比例)
* **缩放转换**: 前端代码中的所有像素尺寸、坐标定位、字号，均需为 **Figma 设计稿的 1/2 (0.5x)**。

---

## 2. 导航栏与排版安全区域

当前项目集成了全局顶部导航栏 (`ChapterNav.jsx`)，所有内容页面的排版需以此导航栏为对齐基准：

### 2.1 左右边距与水平对齐 (Horizontal Alignment)
* **规范**: 内容区域左右边距必须 **紧贴导航栏左右** 边界。
* **实现**: 
  - 导航栏使用的 padding 为 `px-12 sm:px-16`。在 `1920px` 宽度下，这对应于两侧各有 `64px` 的外边距。
  - 所有幻灯片页面内容区域的左右 padding/margin 必须统一设为 `px-12 sm:px-16` (即 `64px` 边距)。
  - 内容区域宽度为 `1920 - 64 * 2 = 1792px`，严禁使用 `max-w-[1500px]` 等导致宽度不一致的固定最大宽度限制。

### 2.2 顶部位置与垂直对齐 (Vertical Alignment - Content Top)
* **规范**: 页面内容顶部 (Content Top) 必须 **刚好放在导航栏下面** (距离视口顶部为 `160px`)。
* **实现**:
  - 全局布局组件 `ChapterPage.jsx` 会自动将内容页面渲染于导航栏下方的 `flex-1` 容器中，设置有 `mt-8` (32px 间距)，内容顶部刚好对齐 `160px` 辅助线。
  - 内容页面顶部不应再包含大片空白区域，主标题栏或卡片区直接与导航栏下边界紧密承接。

### 2.3 底部避让区 (Subtitle Area) - ⚠️ 严禁排版文字
* **规范**: 屏幕底部往上 `40px` 的区域为实时会议字幕避让区。**绝对禁止**在此区域内排版任何关键文字、列表或图表内容。

### 2.4 排版辅助参考线切换 (Guidelines)
为了在浏览器中实时确认排版元素是否越界或对齐：
* **按键切换**：在幻灯片运行页面中，直接在键盘上按下 **`G` 键**，即可打开/关闭蓝色虚线排版辅助参考线和底部的红色避让区警示带。

---

## 3. 页面标题 (Page Header) 规范
* **样式**: 
  - 页面标题应采用左对齐，与导航栏左侧的章节标题保持垂直对齐线一致。
  - 使用 `text-zinc-100` 或类似白色/灰白色。
  - 字号建议为 `text-[28px]` 至 `text-[32px]`，字重 `font-bold`。
  - 顶部与底部 padding 应该保持紧凑（例如 `pt-2 pb-2`），以便给主体内容留出足够的垂直空间，确保页面无需滚动即可在 1080p 下完全显示。

---

## 4. 颜色与视觉强调规范 (Color & Emphasis)
* **主强调色 (Primary Accent Color)**: `#004CE5` (或 Tailwind `text-[#004CE5]` / `bg-[#004CE5]`)。
* **规则**: 统一使用主强调色来修饰核心数据指标、百分比、图表重点区域及关键结论，禁止混用杂乱的色彩。
* **视觉品质**: 使用暗色玻璃态背景 (`bg-zinc-900/50 backdrop-blur-md border border-white/10`)、渐变边框和微动画 hover 效果以营造极具高级感的视觉氛围。
